version: "2"

linters:
    default: standard
    exclusions:
        presets:
            - comments
            - std-error-handling
            - common-false-positives
        paths:
            - vendor
            - bin
            - third_party
            - ".*\\.pb\\.go$"

run:
    timeout: 5m
    tests: true
